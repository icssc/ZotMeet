import { describe, it, expect } from "vitest";

// You'll write tests eventually, right?

describe("sum test", () => {
  it("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).toBe(3);
  });
});

// ...right?
